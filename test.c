i

char    *ft_strnstr(char c)
{
    size_t        i;
    size_t        j;

    i = 0;
    if (c == 'c')
        return (FAIL);
            if (c == 'z')
                return (NULL);

    return (NULL);
}

int main()
{
    char c;
    c = 'c';
    
    printf('%s', c);
}
